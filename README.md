# 🤖 Розпізнавання Зображень з TensorFlow

Веб-додаток для розпізнавання об'єктів на зображеннях за допомогою штучного інтелекту, побудований на Django та TensorFlow.

**Генеровано AI:** Claude Haiku 4.5

---

## 📋 Функціональність

-   **Завантаження зображень** - підтримка Drag & Drop та вибір файлу
-   **Розпізнавання об'єктів** - миттєве визначення об'єктів на зображенні
-   **Топ-5 результатів** - відображення 5 найімовірніших передбачень
-   **Процентне відображення впевненості** - точна оцінка точності розпізнавання
-   **Сучасний інтерфейс** - адаптивний дизайн з градієнтним фоном
-   **Обробка помилок** - зручні повідомлення про помилки користувачу

---

## 🏗️ Архітектура Проекту

### Основні компоненти:

#### 1. **imagerecognition/models.py** - Логіка розпізнавання

```
ImageRecognizer - клас для роботи з нейронною мережею
├── get_model() - завантаження або повернення закешованої моделі
├── recognize_image() - основна функція розпізнавання
└── Використовує MobileNetV2 з ImageNet
```

**Особливості:**

-   Кешування моделі в пам'яті для швидкої обробки
-   Обробка SSL сертифікатів
-   Розпізнавання 1000+ класів об'єктів

#### 2. **imagerecognition/views.py** - API та логіка запитів

```
index() - відображення головної сторінки
recognize() - обробка завантажених зображень
├── Валідація типу файлу
├── Тимчасове збереження файлу
├── Виклик розпізнавання
└── Видалення тимчасових файлів
```

**Функції:**

-   CSRF захист для безпеки
-   Підтримка форматів: JPG, PNG, GIF, BMP
-   Обробка помилок з детальними повідомленнями
-   JSON API для фронтенда

#### 3. **imagerecognition/urls.py** - Маршрутизація

```
'' → index() - головна сторінка
'recognize/' → recognize() - API для розпізнавання
```

#### 4. **imagerecognition/templates/imagerecognition/index.html** - Фронтенд

```
📱 Користувацький інтерфейс
├── Область завантаження (Drag & Drop)
├── Попередній перегляд зображення
├── Область завантаження
├── Індикатор завантаження
├── Список результатів
└── Обробка помилок
```

**Технології:**

-   HTML5
-   CSS3 з градієнтами та анімаціями
-   Vanilla JavaScript (без фреймворків)
-   CSRF token для безпеки

#### 5. **mysite/settings.py** - Конфігурація Django

```
INSTALLED_APPS:
├── imagerecognition - наш додаток
├── polls - додаток для опитувань
├── debug_toolbar - інструменти розробки
└── Django системні додатки

MEDIA_ROOT - директорія для завантажених файлів
MEDIA_URL - URL для доступу до медіа
```

#### 6. **mysite/urls.py** - Основна маршрутизація

```
/imagerecognition/ → imagerecognition.urls
/polls/ → polls.urls
/admin/ → Django адмін-панель
/__debug__/ → Debug toolbar
```

#### 7. **requirements.txt** - Залежності проекту

```
Django 6.0.1 - веб-фреймворк
TensorFlow 2.20.0 - глибоке навчання
Keras - API для нейронних мереж
MobileNetV2 - модель штучного інтелекту
Pillow - обробка зображень
NumPy - числові обчислення
OpenCV - комп'ютерний зір
django-debug-toolbar - інструменти розробки
```

---

## 🛠️ Технічний Стек

| Компонент | Технологія         | Версія               |
| --------- | ------------------ | -------------------- |
| Фреймворк | Django             | 6.0.1                |
| AI/ML     | TensorFlow         | 2.20.0               |
| Модель    | MobileNetV2        | Pre-trained ImageNet |
| Мова      | Python             | 3.12.10              |
| ОС        | macOS              | -                    |
| Сервер    | Django Development | Built-in             |

---

## 📁 Структура Файлів

```
ai-app/
├── imagerecognition/           # Додаток розпізнавання
│   ├── models.py               # Логіка AI розпізнавання
│   ├── views.py                # Обробка запитів
│   ├── urls.py                 # Маршрутизація
│   ├── apps.py                 # Конфіг додатку
│   ├── admin.py                # Django адмін
│   ├── tests.py                # Тести
│   └── templates/
│       └── imagerecognition/
│           └── index.html      # Головна сторінка (UI)
│
├── mysite/                      # Основний проект Django
│   ├── settings.py              # Конфігурація
│   ├── urls.py                  # Основна маршрутизація
│   ├── wsgi.py                  # WSGI додаток
│   └── asgi.py                  # ASGI додаток
│
├── media/                       # Завантажені файли
│   └── temp/                    # Тимчасові файли
│
├── keras_cache/                 # Кеш моделей TensorFlow
│   ├── keras.json
│   └── models/
│
├── polls/                       # Додатковий додаток (опитування)
├── manage.py                    # Команди Django
├── db.sqlite3                   # База даних
├── requirements.txt             # Python залежності
└── README.md                    # Документація англійською

```

---

## 🚀 Як Користуватись

### Запуск сервера:

```bash
cd /Users/vd/IT/ai-app
python3 manage.py runserver
```

### Доступ до додатку:

```
http://127.0.0.1:8000/imagerecognition/
```

### Використання:

1. **Завантажте зображення** - перетягніть файл або натисніть на область
2. **Дочекайтеся обробки** - індикатор показуватиме прогрес
3. **Прочитайте результати** - отримайте список розпізнаних об'єктів з відсотками впевненості

---

## 🧠 Нейронна Мережа MobileNetV2

-   **Тип:** Convolutional Neural Network (CNN)
-   **Параметри:** ~3.5M (легка версія)
-   **Вхідне зображення:** 224x224 пікселів
-   **Вихід:** 1000 класів об'єктів (ImageNet)
-   **Точність:** Top-1 ~71.3%, Top-5 ~90.1%

**Переваги MobileNetV2:**

-   Швидке розпізнавання в реальному часі
-   Низькі вимоги до обчислювальних ресурсів
-   Вміст на мобільних пристроях
-   Попередньо навчена на мільйонах зображень

---

## 🔒 Безпека

-   **CSRF захист** - для всіх POST запитів
-   **Валідація файлів** - перевірка типу та формату
-   **Тимчасові файли** - автоматична видалення після обробки
-   **SSL сертифікати** - безпечне завантаження моделей

---

## 📝 Примітки

-   Перше завантаження моделі може зайняти час (420MB)
-   Модель кешується локально для прискорення наступних запитів
-   Потрібне інтернет-з'єднання для першого завантаження
-   Додаток використовує Python 3.12.10

---

**Розроблено з використанням AI:** Claude Haiku 4.5  
**Дата створення:** 16 січня 2026 р.
